import React from 'react';
import ReactDOM from 'react-dom';

import App from './App';




ReactDOM.render(<App />, document.querySelector('#root'));
// the above line means that everything we create in react will be in App and that will be forced into the div with id "root".

